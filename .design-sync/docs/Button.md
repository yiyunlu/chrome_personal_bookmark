---
category: Actions
---

# Button

The one button. `default` is the single primary action of a surface, `outline` secondary, `ghost` icon-only and row actions, `destructive` a confirmed destructive action, `link` inline text. Icons inside are sized by the cva (`[&_svg]:size-4`); pass no size prop to them.
